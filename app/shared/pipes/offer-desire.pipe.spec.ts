import { OfferDesirePipe } from './offer-desire.pipe';

describe('OfferDesirePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferDesirePipe();
    expect(pipe).toBeTruthy();
  });
});
